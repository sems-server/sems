#
# AWS access key (see aws website)
aws_access_key=YOUR_AWS_ACCESS_KEY_HERE

#
# AWS secret (see aws website)
aws_secret_access_key=YOUR_AWS_ACCESS_SECRET_HERE

# how many connections to S3 to create initially
# (connectionpool creates connections on demand)
#
# Default:s3_initial_connections=0
#
#s3_initial_connections=3

# how many connections to SQS to create initially
# (connectionpool creates connections on demand)
#
# Default:sqs_initial_connections=0
#
#sqs_initial_connections=3
